# SuperGuard Core - Environment Variables Example
# Скопируйте в .env и заполните своими значениями

# =============================================================================
# База данных (PostgreSQL)
# =============================================================================
SG_DATABASE_URL=postgresql+asyncpg://superguard:superguard123@localhost:5432/superguard

# =============================================================================
# Redis (Event Bus, кэширование, rate limiting)
# =============================================================================
SG_REDIS_URL=redis://localhost:6379/0

# =============================================================================
# Хранилище медиафайлов
# =============================================================================
SG_STORAGE_PATH=/var/lib/superguard/storage

# =============================================================================
# Сервер
# =============================================================================
SG_HOST=0.0.0.0
SG_PORT=8000
SG_WORKERS=4
SG_DEBUG=false
SG_LOG_LEVEL=INFO

# =============================================================================
# Безопасность (JWT)
# =============================================================================
# Сгенерируйте ключи: openssl genrsa -out jwt_private.pem 2048 && openssl rsa -in jwt_private.pem -pubout -out jwt_public.pem
SG_SECRET_KEY=your-super-secret-key-change-in-production-min-32-chars
SG_JWT_PRIVATE_KEY=/etc/superguard/jwt_private.pem
SG_JWT_PUBLIC_KEY=/etc/superguard/jwt_public.pem

# =============================================================================
# Telegram Bot (для уведомлений)
# =============================================================================
# Получите токен у @BotFather
SG_TELEGRAM_BOT_TOKEN=your-bot-token-here
# Разрешенные пользователи (user_id), можно узнать через @userinfobot
SG_TELEGRAM_ALLOWED_USERS=123456789,987654321

# =============================================================================
# Tuya Cloud (fallback для розеток)
# =============================================================================
# Получите в Tuya IoT Platform: https://iot.tuya.com
SG_TUYA_CLIENT_ID=your-tuya-client-id
SG_TUYA_CLIENT_SECRET=your-tuya-client-secret
SG_TUYA_REGION=eu  # us, eu, cn, in

# =============================================================================
# MQTT (опционально)
# =============================================================================
SG_MQTT_HOST=localhost
SG_MQTT_PORT=1883
SG_MQTT_USERNAME=
SG_MQTT_PASSWORD=

# =============================================================================
# MediaMTX (RTSP/HLS сервер)
# =============================================================================
SG_MEDIAMTX_URL=http://localhost:9997

# =============================================================================
# Prometheus метрики
# =============================================================================
SG_METRICS_ENABLED=true